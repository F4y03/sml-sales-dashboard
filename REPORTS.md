# ดูรายงาน SML ผ่านเว็บ

เปิด http://localhost:3001/reports.html หรือกด **เปิดรายงาน SML** บน Dashboard

1. ค้นหาชื่อหรือรหัสรายงาน เช่น 1002, 2017 หรือ 4007
2. เลือกรายงาน รายการและเงื่อนไขอ่านจาก `sml_fastreport` ในฐานจริง
3. เลือก **วันนี้**, **เมื่อวาน** หรือวันที่ใน **เลือก 1 วัน** แล้วกด **ใช้วันนี้กับรายงาน**
4. กำหนดคลัง/รหัสสินค้าเพิ่มเติม หรือเว้นว่างเพื่อดูทั้งหมด แล้วกด **แสดงรายงาน**

สำหรับรายงานเคลื่อนไหว ช่วง 1 วันคือวันเริ่มต้นและวันสิ้นสุดตรงกัน สำหรับรายงานยอดคงเหลือหมายถึงยอดสะสมถึงวันนั้นตามสูตร SML ไม่ใช่จำนวนเคลื่อนไหวเฉพาะวัน รายงานที่ไม่มีเงื่อนไขวันที่จะปิดตัวเลือกวัน

รายงานหลายชุดข้อมูลมีตัวเลือก **ชุดข้อมูล** เพื่อดูแต่ละชุด เช่นหัวเอกสารและรายละเอียด ตารางแสดงครั้งละ 100 แถว กดก่อนหน้า/ถัดไปเพื่อดูต่อได้ CSV และพิมพ์/PDF ส่งออกเฉพาะหน้าที่แสดง พร้อมรหัสรายงานและเงื่อนไข ข้อมูลถูกดึงใหม่เมื่อกดแสดงรายงานหรือเปลี่ยนหน้า ไม่ได้เก็บ snapshot ข้ามหน้า หากมีการแก้ข้อมูลระหว่างดู หน้าอาจเปลี่ยนตามฐานจริง

## ขอบเขต

แสดงรายชื่อครบ 569 รายการที่พบในฐาน สร้างฟอร์มและหัวคอลัมน์จาก XML ที่บีบอัดในฐาน และใช้ SQL/ฟังก์ชันรายงานเดิม รูปแบบผลลัพธ์เป็นตารางเว็บ ไม่ใช่การจำลองแบบพิมพ์/บาร์โค้ด/การจัดกลุ่มหลายระดับของโปรแกรม SML

รองรับพารามิเตอร์วันที่ เวลา ตัวเลข ตัวเลือก รหัสเดี่ยว หลายรหัสคั่นด้วย comma และช่วงรหัสคั่นด้วย colon การแทนค่า SQL ใช้ parameters ฝั่ง server เท่านั้น client ไม่สามารถส่ง SQL หรือชื่อตารางเอง

**ไม่ได้รับรองว่ารันได้ทุก 569 รายการ** บางรายงานมี macro ของโปรแกรม เช่นสาขาปัจจุบัน หรือสูตร/คำสั่งเฉพาะที่ต้องเพิ่มตัวแปลง รายงานเหล่านั้นแจ้งข้อผิดพลาดชัดเจน ไม่มีการข้ามเงื่อนไขที่ไม่รู้จักหรือใช้สูตรแทน หากรันเกิน 25 วินาทีให้จำกัดช่วงวันที่/รหัสสินค้า

รายงานที่ทดสอบกับฐานจริงและเปิดผลสำเร็จ: 1002, 2017, 4007, 4014 พร้อมทดสอบตัวกรองสินค้า/คลังของ 2017, pagination, เลือก 1 วัน, CSV และหน้าจอมือถือ ไม่มีการเขียนข้อมูลลง SML การประมวลผลใช้ transaction read-only

## ทดสอบ

```powershell
$env:TEST_URL='http://localhost:3001'
node test-reports.js
```

ใช้ Chrome ที่ติดตั้งในเครื่องและ @playwright/test ซึ่งอยู่ใน devDependencies การทดสอบปิดคำขอฟอนต์ภายนอกเพื่อไม่ให้การเชื่อมต่อ Google Fonts ทำให้ทดสอบค้าง

API: `GET /api/reports`, `GET /api/reports/:key`, `POST /api/reports/:key/run` โดย key คือ roworder ที่ได้จากรายการ ไม่ใช่ menuid เพื่อแยกรายงานที่มีรหัสเมนูซ้ำ ตัวอย่าง body: `{"filters":{"from_date":"2026-09-07","to_date":"2026-09-07","from_time":"","to_time":""},"dataset":0,"page":0,"pageSize":100}` สำหรับ 4007
