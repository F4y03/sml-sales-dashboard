# แหล่งข้อมูลสินค้าฝาก

ฐานข้อมูล ski เก็บสินค้าฝากเป็น `ic_inventory.code` ขึ้นต้น `ฝ` เช่น `ฝหย055UK12` ไม่ใช่ `ar_customer.code` และไม่ใช้โมดูล `ic_wms_trans_detail` สำหรับข้อมูลชุดนี้

API `/api/consignment` ใช้ `ic_trans_detail` ตามเงื่อนไขรายงาน 2060 “รายงานเคลื่อนไหวสินค้า ตามคลัง” (sml_fastreport roworder 1132): last_status=0, เฉพาะประเภทที่กระทบสต็อก, ยกเว้น POS ที่มี doc_ref และแปลงหน่วยด้วย stand_value/divide_value คงเหลือสะสมถึงวันที่ปัจจุบันด้วย qty × calc_flag × อัตราแปลงหน่วย จำนวนออกใช้ calc_flag=-1 รวมรายการขายที่ตัดสต็อกสินค้าฝากด้วย ไม่ใช่เฉพาะใบเบิก 56

กลุ่มลูกค้าแยกจากรูปแบบ `^ฝ[^0-9]{2}[0-9]{3}` เช่น ฝหย055 หากไม่ตรงรูปแบบใช้รหัสสินค้าเต็ม ไม่เติมเลขหรือเดาลูกค้า ภูมิภาคใช้ mapping ตามคำขอ รหัสอื่น เช่น ฝวย/ฝลอ แสดงไม่ระบุภูมิภาค

มีตัวกรองหน่วย เพราะยอดรวมเริ่มต้นเป็นจำนวนรวมต่างหน่วย กราฟโดนัทแสดง 10 อันดับแรกและรวมกลุ่มที่เหลือ ตารางสรุปแสดงครบ ตัวกรองทั้งหมดควบคุม KPI และกราฟร่วมกัน

ตรวจ 2026-09-14: 36,314 รายการ / 2,997 รหัสสินค้าที่เคลื่อนไหว / 203 กลุ่มรหัสฝาก เบิกออกสะสม 2,936,020 คงเหลือ 247,420 เปรียบเทียบยอดคงเหลือรายรหัสกับทะเบียนสินค้าขึ้นต้น ฝ ทั้ง 3,108 รหัส ตรงทุกตัว (รวมสินค้าที่ไม่มีการเคลื่อนไหวและคงเหลือศูนย์) ตัวเลขเปลี่ยนตามข้อมูลสด

ทดสอบ: `node --test test-consignment.js test-consignment-loading.js` และตรวจหน้าเว็บด้วยข้อมูลจริงผ่าน Playwright: KPI, กราฟทั้งสาม, ตัวกรองลูกค้า/หน่วย และความกว้างมือถือ ผ่านโดยไม่มี JavaScript error
